name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Build web UI
        run: cd web && npm ci && npm run build

      - name: Prepare embedded static files
        run: rm -rf cmd/blackwood/static && cp -r web/dist cmd/blackwood/static

      - name: Build
        run: go build -o bin/blackwood ./cmd/blackwood

      - name: Test
        run: make test

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Ensure embedded static dir exists
        run: mkdir -p cmd/blackwood/static && touch cmd/blackwood/static/.gitkeep

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: latest
